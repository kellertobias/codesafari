---
slug: fizzbuzz
title: "Example: FizzBuzz (TypeScript)"
order: 10
defaultSnippetLines: 12
---

A tiny [`.ts`](examples/typescript/fizzbuzz.ts) example — the classic FizzBuzz —
showing how a tour anchors to plain TypeScript functions. Two steps: the
classifier that turns a number into its word, and the loop that drives a range
through it.
