---
slug: counter
title: "Example: Counter (React / TSX)"
order: 12
defaultSnippetLines: 12
---

A minimal React counter in [`.tsx`](examples/react/Counter.tsx). Three steps
cover local state with `useState`, updating from the previous value, and
rendering JSX as a function of that state.
